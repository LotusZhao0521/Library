# 服务地址配置
BACKEND_HOST=0.0.0.0
# 后端端口（必须与 backend/.env 中的 PORT 保持一致）
# 提示：修改端口时，只需修改 backend/.env 中的 PORT，然后运行 ./sync-env.sh 同步
PORT=8000

# 前端配置
# 前端容器内监听端口（nginx 监听端口，默认 80）
FRONTEND_PORT=80
# 前端主机映射端口（外部访问端口，建议使用非标准端口如 18081、8080、3000 等）
# 安全提示：避免使用 80/443 等标准端口，降低被自动扫描的风险
# 公网部署示例：设置为 18081 时，访问 http://你的公网IP:18081
# 例如：设置为 18081 时，访问 http://localhost:18081 或 http://123.45.67.89:18081
FRONTEND_HOST_PORT=18081
# 前端域名（nginx server_name，默认 _ 表示接受所有域名）
# 例如：设置为 library.example.com 时，只接受该域名的请求
# 设置为 _ 表示接受所有域名/IP 的请求（公网 IP 访问时保持为 _）
FRONTEND_DOMAIN=_

# 开发环境前端端口（Vite 开发服务器端口）
FRONTEND_DEV_PORT=5173
